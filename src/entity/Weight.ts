/*import {Entity, Column} from 'typeorm';

import Bias from './Bias';
import Computer from './Computer';

@Entity()
export default class Weight extends Bias {

    @Column()
    to : number;

    constructor(startingIndex : number, endingIndex : number, layer : number, hostComputer : Computer){
        super(startingIndex, layer, hostComputer);
        this.to = endingIndex;
    }

}*/
